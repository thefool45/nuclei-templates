id: wix-detect

info:
  name: Wix Detection
  author: Chirag Mistry
  severity: info
  description: |
    An instance running Wix is detected by looking for specific HTTP headers, HTML tags, and known endpoints.
  metadata:
    verified: true
    max-request: 8
  tags: wix,detect,tech

http:
  - method: GET
    path:
      - "{{BaseURL}}"
      - "{{BaseURL}}/robots.txt"
      - "{{BaseURL}}/sitemap.xml"
      - "{{BaseURL}}/favicon.ico"
      - "{{BaseURL}}/_static/"
      - "{{BaseURL}}/wix-api"
      - "{{BaseURL}}/user/login"
      - "{{BaseURL}}/wix.js"

    stop-at-first-match: true
    matchers-condition: and
    matchers:
      - type: word
        part: response
        words:
          - "Powered by Wix"
          - "X-Wix-Request-Id"
          - "Wix Static Site"
          - "wixsite.com"
        condition: or
        case-insensitive: true

      - type: status
        status:
          - 200
          - 403
          - 404
        condition: or

    extractors:
      - type: regex
        part: body
        name: version
        regex:
          - 'Wix.com ([0-9.]+)'
          - 'Wix version ([0-9.]+)'
          - 'Wix Static Site Version ([0-9.]+)'

      - type: regex
        part: body
        name: wix_id
        regex:
          - 'wix-id=([a-z0-9]+)'
          - 'siteid="([a-zA-Z0-9]+)"'
# digest: 490a004630440220320c0a6b2eb2978d7571e28a76e9ab3a6a0d055befcfc83ce0c4e20cf6755a8502207884b4cff75c20c51973692dd4f8ee9a71670f104758e294a79f99d00ce1c9d4:922c64590222798bb761d5b6d8e72950